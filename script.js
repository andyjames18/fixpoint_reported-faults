
function exportToExcel() {
  alert("Excel export triggered (placeholder).");
}

// Simulate loading data
document.addEventListener("DOMContentLoaded", () => {
  document.getElementById("faultsTable").innerHTML = "<p>No faults reported yet.</p>";
});
